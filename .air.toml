root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "tmp/server"
  include_ext = ["go"]
  exclude_dir = ["tmp", "uploads", "vendor"]
  delay = 500

[log]
  time = true

[misc]
  clean_on_exit = true
